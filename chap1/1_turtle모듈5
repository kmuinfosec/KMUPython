import turtle
s = turtle.Screen()
t = turtle.Turtle()
# 펜 진하게
t.pensize(3)

# 점 그리기
t.dot(30)

x = -100
y = 100
t.penup()
t.goto(x, y)
t.pendown()

# 반지름 100짜리 원 그리기.
# 거북이가 바라보는 방향의 왼쪽이 원중심
t.circle(100)

t.penup()
t.goto(x - 60.62, y + 65)
t.pendown()
t.setheading(-60)
t.circle(70, 120)
